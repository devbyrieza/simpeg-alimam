const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); const bcrypt = require('bcryptjs'); async function main() { const email = 'super@pesantren-alimam.com'; const password = 'AdminAlimam2026!'; const profile = await prisma.profile.findFirst({ where: { OR: [ { email }, { phone: email } ] } }); if (!profile) { console.log('Profile not found'); return; } console.log('Profile found:', profile.email); const isValid = await bcrypt.compare(password, profile.password_hash); console.log('Password valid:', isValid); } main().finally(() => prisma.$disconnect());
